const operations = {
    add: (num1, num2) => {
        return num1 + num2
    },

    subtract: () => {
        return num1 - num2; 
    },

    multiply: () => {
        return num1 * num2;
    },

    divide: () => {
        return num1 / num2;
    }
}

describe("Math Tests", () => {
    
    it("Can add", () => {
        return num1 + num2;
    })

    it("Can subtract", () => {
        return num1 - num2;
    })

    it("Can multiply", () => {
        return num1 * num2;
    })

    it("Can divide", () => {
        return num1 / num2;
    })
})